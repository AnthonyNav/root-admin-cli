# deps.txt - Project package baseline.
# Target platform: AlmaLinux 9 (RHEL-compatible).
#
# Validate the environment with:
#   bash setup.sh
#
# Install the listed packages manually with:
#   sudo dnf install $(grep -v '^#' deps.txt | tr '\n' ' ')
#
# Most of these packages are already present on a minimal AlmaLinux 9 install.
# setup.sh validates command availability and reports anything missing.

shadow-utils
procps-ng
coreutils
util-linux
bash

# Terminal UI tools.
# gum is installed from https://repo.charm.sh/yum/ by main.sh.
# figlet and fzf are available through dnf.
figlet
fzf
